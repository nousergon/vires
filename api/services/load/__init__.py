"""Physiological training-load models.

Currently the Pandolf load-carriage metabolic model (:mod:`api.services.load.pandolf`)
that turns a ruck's pack weight + route aggregates into a metabolic cost — the one
number generic route apps cannot produce because they never capture carried load.
"""
